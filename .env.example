# Only set true for the public browser-only sandbox.
VITE_DEMO_MODE=false
# Optional backend PostgreSQL configuration (export in backend shell):
# DATABASE_URL=jdbc:postgresql://127.0.0.1:5433/reconciliation
# DATABASE_USER=recon
# DATABASE_PASSWORD=local-demo-only
